This is a backup copy of my GearSwap lua files, including some helper functions and slightly modified Mote include files.

Many shared helper functions are placed in ``user-globals.lua``,
and some job files include other files from the ``gearswawp-libs/`` folder as well.
